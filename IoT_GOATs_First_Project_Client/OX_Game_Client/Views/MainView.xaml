<mah:MetroWindow x:Class="OX_Game_Client.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:OX_Game_Client.Views"
        xmlns:vm="clr-namespace:OX_Game_Client.ViewModels"
        mc:Ignorable="d"
        Title="죽음의 OX 게임" Height="450" Width="700">
    <Grid Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center">
        <!-- 유저 이름 입력란 -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 유저 이름 라벨 -->
        <Label Content="유저 이름" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0"/>

        <!-- 유저 이름 입력 텍스트박스 -->
        <TextBox x:Name="UserNameTextBox"  Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Width="200" Height="30" Margin="0,0,0,40" Grid.Row="1"/>
        <Button Content="확인" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="40" 
                Command="{Binding TestSubmitCommand}"
                Grid.Row="2" />

        <!--<Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" Source="./Views/Empty.xaml" 
               Grid.RowSpan="3" Visibility="Collapsed"/>-->
        <Frame x:Name="MainFrame" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Collapsed"/>
    </Grid>
</mah:MetroWindow>
